import { BaseController } from './BaseController'; import { CategoryService } from '../services/CategoryService'; import { CatalogProductService } from '../services/CatalogProductService';
export class CategoryController extends BaseController { private s=new CategoryService(); list(q?:string){return this.execute(()=>this.s.list(q))} get(id:number){return this.execute(()=>this.s.get(id))} create(d:unknown){return this.execute(()=>this.s.save(d))} update(id:number,d:unknown){return this.execute(()=>this.s.save(d,id))} setActive(id:number,a:boolean){return this.execute(()=>{this.s.setActive(id,a);return null})} }
export class ProductController extends BaseController { private s=new CatalogProductService(); list(q?:string,c?:number,l?:boolean){return this.execute(()=>this.s.list(q,c,l))} get(id:number){return this.execute(()=>this.s.get(id))} create(d:unknown){return this.execute(()=>this.s.save(d))} update(id:number,d:unknown){return this.execute(()=>this.s.save(d,id))} setActive(id:number,a:boolean){return this.execute(()=>{this.s.setActive(id,a);return null})} barcode(b:string){return this.execute(()=>this.s.findByBarcode(b))} }
