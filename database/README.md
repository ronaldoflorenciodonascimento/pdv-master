# Banco de dados

As migrations SQL ficam em `migrations/` e são aplicadas automaticamente pelo `MigrationRunner` na inicialização do aplicativo. Consulte [o modelo completo](../docs/DATABASE.md) para entidades, relacionamentos e convenções.
